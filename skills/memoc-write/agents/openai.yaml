interface:
  display_name: "Memoc Write"
  short_description: "Operate memoc notes safely through the CLI"
  default_prompt: "Use $memoc-write to read or update a memoc note through the versioned JSON CLI."
